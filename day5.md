## Day 5 - Linux Process Management

### Commands learned:
- ps aux → see all running processes
- top / htop → live process viewer
- pgrep → find process ID by name
- kill [PID] → kill a specific process
- killall → kill all processes by name
- jobs → see background jobs
- & → run process in background
- fg / bg → foreground / background

### Mini challenge:
- Completed / Struggled with that apache2 is not starting in terminal 

### Key learning:
- Every running program is a process
- Each process has a unique PID number
- kill command uses PID to stop processes
